{
  "name": "trwm-draft-submission-skill",
  "version": "0.20.0",
  "description": "A skill that drafts a SOLVE-IT knowledge base submission through the TRWM-AC method, for Claude Code and the Claude desktop and web applications",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "test": "node --test",
    "schema": "node skills/trwm-draft-submission/package_session.mjs --schema > skills/trwm-draft-submission/references/draft.schema.json",
    "set-version": "node scripts/set-version.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
